<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.questory.repository.PostRepository">

    <insert id="create" parameterType="com.ssafy.questory.domain.Post">
        INSERT INTO Posts (
        user_email,
        title,
        content,
        created_at,
        category
        ) VALUES (
        #{userEmail},
        #{title},
        #{content},
        NOW(),
        #{category}
        )
    </insert>
    <update id="update" parameterType="com.ssafy.questory.domain.Post">
        UPDATE Posts
        SET title = #{title},
        content = #{content},
        category = #{category},
        updated_at = NOW()
        WHERE post_id = #{postId} AND user_email = #{userEmail}
    </update>
    <select id="findById" resultType="com.ssafy.questory.domain.Post" parameterType="long">
        SELECT * FROM Posts WHERE post_id = #{postId}
    </select>
</mapper>
